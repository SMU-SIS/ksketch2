<?xml version="1.0" encoding="utf-8"?>
<s:Group xmlns:fx="http://ns.adobe.com/mxml/2009"
		 xmlns:s="library://ns.adobe.com/flex/spark"
		 xmlns:buttons="sg.edu.smu.ksketch2.canvas.components.buttons.*" initialize="initStyle()" 
		 width="100%" height="100%">
	
	<fx:Script>
		<![CDATA[
			import mx.events.FlexEvent;
			
			import sg.edu.smu.ksketch2.KSketchAssets;
			import sg.edu.smu.ksketch2.KSketchStyleSheet;
			import sg.edu.smu.ksketch2.utils.KSwipeEvent;
			
			private function initStyle():void
			{
				this.removeEventListener(FlexEvent.INITIALIZE, initStyle);
				
				logButton.init(KSketchAssets.actionList_log , KSketchAssets.actionList_log);
				saveButton.init(KSketchAssets.actionList_save , KSketchAssets.actionList_save);
				deleteButton.init(KSketchAssets.actionList_delete , KSketchAssets.actionList_delete);
				logButton.initSkin();
				saveButton.initSkin();
				deleteButton.initSkin();
			}
			
			protected function button_deleteHandler(event:MouseEvent):void
			{
				stage.dispatchEvent(new KSwipeEvent(KSwipeEvent.DELETE_ACTION));
			}
			
			protected function button_logHandler(event:MouseEvent):void
			{
				stage.dispatchEvent(new KSwipeEvent(KSwipeEvent.LOG_ACTION));
			}
			
			protected function button_saveHandler(event:MouseEvent):void
			{
				stage.dispatchEvent(new KSwipeEvent(KSwipeEvent.SAVE_ACTION));
			}
		]]>
	</fx:Script>
	<s:HGroup verticalAlign="top" horizontalAlign="right" width="100%" height="100%" gap="{KSketchStyleSheet.LIST_SKETCH_GAP}" id="listButtons">
		<buttons:KSketch_ListButton id="logButton" width="{KSketchStyleSheet.LIST_SKETCH_WIDTH}" height="{KSketchStyleSheet.LIST_SKETCH_HEIGHT}" click="button_logHandler(event)"/>
		<buttons:KSketch_ListButton id="saveButton" width="{KSketchStyleSheet.LIST_SKETCH_WIDTH}" height="{KSketchStyleSheet.LIST_SKETCH_HEIGHT}" click="button_saveHandler(event)"/>
		<buttons:KSketch_ListButton id="deleteButton" width="{KSketchStyleSheet.LIST_SKETCH_WIDTH}" height="{KSketchStyleSheet.LIST_SKETCH_HEIGHT}" click="button_deleteHandler(event)"/>
	</s:HGroup>
</s:Group>
