<?xml version="1.0" encoding="utf-8"?>
<s:SkinnablePopUpContainer xmlns:fx="http://ns.adobe.com/mxml/2009" 
		 xmlns:s="library://ns.adobe.com/flex/spark" width="100%" height="100%" backgroundAlpha="0"
		 x="{(Capabilities.screenResolutionX/2) - (width/2)}">
	<fx:Script>
		<![CDATA[
			import mx.core.UIComponent;
			private var timer:Timer;
			private var _parentContainer:UIComponent;
			
			public function init(parentContainer:UIComponent):void
			{
				timer = new Timer(250);
				timer.addEventListener(TimerEvent.TIMER, _closeMessage);
				_parentContainer = parentContainer;
			}
			
			public function showMessage(message:String):void
			{				
				if(_parentContainer)
				{
					timer.reset();
					timer.start();
					open(_parentContainer);
					messageText.text = message;
				}
			}
			
			private function _closeMessage(event:TimerEvent):void
			{
				close();
			}
		]]>
	</fx:Script>
	<s:Group width="100%">
		<s:layout>
			<s:VerticalLayout/>
		</s:layout>
		<s:Group height="{Capabilities.screenResolutionY/2}"/>
		<s:Label id="messageText" fontSize="64" fontFamily="_sans"/>
	</s:Group>
</s:SkinnablePopUpContainer>
